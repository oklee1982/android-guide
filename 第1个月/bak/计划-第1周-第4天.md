好的，按照第1周路线整理出第4天的详细执行清单，专注 RecyclerView 与列表交互，便于直接照表推进。

今日目标

- 熟练掌握 RecyclerView 的 Adapter/ViewHolder/布局管理器基础。
- 接入列表点击与长按事件，完成与 SecondActivity 的跳转贯通。
- 初步使用 DiffUtil 管理列表刷新，认识其性能优势。
- 梳理列表常见问题与排查手段。

建议安排（8 小时）

- 第 1 小时：RecyclerView 入门讲义与练习题
- 第 2-4 小时：接入 RecyclerView + Adapter + 点击跳转
- 第 5 小时：文档与排查清单阅读
- 第 6-7 小时：删除功能 + DiffUtil 实战 + 空态视图
- 第 8 小时：复盘 + 代码审查 + 面试题

第 1 小时：RecyclerView 讲义（AI 讲义 + 练习）

1) 提示词：

```
请输出RecyclerView入门讲义：Adapter/ViewHolder、LayoutManager、ItemDecoration、点击事件与DiffUtil基础。要求提供最小可运行示例（Kotlin），并附3道练习题与答案解析。
```

2) 追加任务：

- “请给出 5 个 DiffUtil 判断题（含解析），强调 areItemsTheSame 与 areContentsTheSame 的差异。”
- “请列出 RecyclerView item 点击、长按、双击的实现对比，以及需要注意的复用问题。”

3) 跟练示例 Adapter：

```kotlin
class UserAdapter(
    private val onClick: (User) -> Unit,
    private val onLongClick: (User) -> Unit
) : ListAdapter<User, UserAdapter.UserViewHolder>(DiffCallback) {

    companion object DiffCallback : DiffUtil.ItemCallback<User>() {
        override fun areItemsTheSame(oldItem: User, newItem: User): Boolean =
            oldItem.name == newItem.name

        override fun areContentsTheSame(oldItem: User, newItem: User): Boolean =
            oldItem == newItem
    }

    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): UserViewHolder {
        val binding = ItemUserBinding.inflate(
            LayoutInflater.from(parent.context), parent, false
        )
        return UserViewHolder(binding)
    }

    override fun onBindViewHolder(holder: UserViewHolder, position: Int) {
        val user = getItem(position)
        holder.bind(user)
    }

    inner class UserViewHolder(
        private val binding: ItemUserBinding
    ) : RecyclerView.ViewHolder(binding.root) {
        init {
            binding.root.setOnClickListener {
                if (bindingAdapterPosition != RecyclerView.NO_POSITION) {
                    onClick(getItem(bindingAdapterPosition))
                }
            }
            binding.root.setOnLongClickListener {
                if (bindingAdapterPosition != RecyclerView.NO_POSITION) {
                    onLongClick(getItem(bindingAdapterPosition))
                    true
                } else {
                    false
                }
            }
        }

        fun bind(user: User) {
            binding.tvName.text = user.name
            binding.tvAge.text = "${user.age} 岁"
            binding.tvCity.text = user.city ?: "未填写"
        }
    }
}
```

第 2-4 小时：接入 RecyclerView + 点击跳转

目标：替换 Day3 的字符串展示，改为 RecyclerView 列表。

步骤建议：

1. 新建 `res/layout/item_user.xml`：

```xml
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="12dp">

    <TextView
        android:id="@+id/tvName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textAppearance="?attr/textAppearanceBodyLarge"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@id/tvAge"
        app:layout_constraintHorizontalBias="0"/>

    <TextView
        android:id="@+id/tvAge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="18 岁"
        app:layout_constraintBaseline_toBaselineOf="@id/tvName"
        app:layout_constraintEnd_toEndOf="parent"/>

    <TextView
        android:id="@+id/tvCity"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="城市：未填写"
        android:textAppearance="?attr/textAppearanceBodySmall"
        app:layout_constraintTop_toBottomOf="@id/tvName"
        app:layout_constraintStart_toStartOf="@id/tvName"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginTop="4dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>
```

2. 将 Day3 的 `tvList` 改为 `RecyclerView`，并在代码中：

```kotlin
private val userAdapter = UserAdapter(
    onClick = { user -> openDetail(user) },
    onLongClick = { user -> confirmDelete(user) }
)

override fun onCreate(savedInstanceState: Bundle?) {
    ...
    binding.rvUsers.apply {
        adapter = userAdapter
        layoutManager = LinearLayoutManager(this@MainActivity)
        addItemDecoration(DividerItemDecoration(context, DividerItemDecoration.VERTICAL))
    }
    userAdapter.submitList(users.toList())
}
```

3. 将 Day3 的 `buildUserSummary` 保留为备用（或改造成调试输出），主要展示走 RecyclerView。

4. 点击事件：调用 Day2 的 `openDetail`（SecondActivity）并传入 Parcelable `User`。

第 5 小时：文档与排查清单

- 提示词：

```
请整理RecyclerView常见问题排查清单（闪烁、复用导致数据错乱、点击穿透、滚动性能、空态/错误态展示），分别给出成因、排查步骤、常见解决方案。最后附2个案例题。
```

- 输出结果写入“RecyclerView 排查手册”笔记，挑选 1-2 个问题尝试模拟（例如点击事件错位）。

第 6-7 小时：删除功能 + DiffUtil + 空态视图

1. 使用 `ListAdapter` + `DiffUtil` 管理数据：

```kotlin
private fun removeUser(user: User) {
    val newList = users.toMutableList().apply { remove(user) }
    users.clear()
    users.addAll(newList)
    userAdapter.submitList(newList)
}
```

2. 在 `confirmDelete` 中弹出确认对话框：

```kotlin
AlertDialog.Builder(this)
    .setTitle("删除用户")
    .setMessage("确认删除 ${user.name} 吗？")
    .setPositiveButton("删除") { _, _ -> removeUser(user) }
    .setNegativeButton("取消", null)
    .show()
```

3. 空态视图：在布局中添加 `ViewStub` 或简单的 `TextView`，当 `users` 为空时显示“暂无用户，请添加”；否则隐藏。

4. 练习 DiffUtil 验证：打印日志观察 `areItemsTheSame` 与 `areContentsTheSame` 调用情况。

第 8 小时：复盘、审查与面试题

A. 复盘模板

- 今日完成的 3 个关键功能；
- 遇到的 2 个列表问题与解决策略；
- DiffUtil 与 notifyDataSetChanged 的差异总结。

B. 代码审查提示词

```
请扮演Android高级开发，审查我第4天的代码：RecyclerView Adapter、DiffUtil 使用、点击/长按事件、空态视图显示。关注点：ViewHolder 复用、payload 机会、列表刷新性能、变量命名、内存泄漏风险。给出问题列表和修改建议。
```

C. 面试题（让 AI 逐题提问并追问）

- RecyclerView 与 ListView 的差异？
- 如何避免 RecyclerView item 点击穿透？
- DiffUtil 如何判断更新范围？其时间复杂度？
- Adapter 中何时使用 `bindingAdapterPosition` 与 `absoluteAdapterPosition`？
- 什么时候需要自定义 `ItemDecoration`？

常见坑与排查清单

- 未设置 `setHasFixedSize(true)` 但布局高度固定导致重新测量，视情况开启。
- `submitList` 默认异步 diff，如需立即刷新请调用 `submitList(newList) { ... }`。
- 长按事件返回 true/false 会影响点击回调，注意逻辑。
- 在 AlertDialog 中使用 Activity context；如果 Activity 已销毁需判断 `isFinishing`。

交付物（第 4 天结束前）

- `item_user.xml` 布局、`UserAdapter`、MainActivity 中的 RecyclerView 配置。
- 删除功能、点击跳转、空态视图逻辑。
- “RecyclerView 排查手册”笔记与练习题答案。
- 今日错题与代码审查反馈整理。

加分项（可选）

- 尝试实现 DiffUtil payload，只更新年龄或城市字段。
- 为 RecyclerView 添加 `SwipeRefreshLayout` 或顶部 header，为后续网络数据做准备。
