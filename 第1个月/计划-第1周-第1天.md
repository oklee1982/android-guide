好的，已按你的要求，将第1周-第1天的内容重发，并把整段代码统一放入代码块中，便于复制与阅读。

今日目标

\- 在Windows上完成Android Studio安装与真机调试

\- Kotlin基础语法入门：变量/类型/字符串模板/if-when/for-while/函数/可空类型与安全调用

\- 创建一个Android项目（Empty Views Activity + ViewBinding），在真机上跑通，完成按钮点击修改TextView

\- 形成第一版“常见问题与解决方案清单”

建议安排（8小时）

\- 第1小时：Kotlin基础讲义与最小例子

\- 第2-3小时：环境与项目创建，真机跑通

\- 第4小时：Kotlin命令行练习

\- 第5小时：Android项目结构与检查清单

\- 第6-7小时：实操（ViewBinding+交互+输入校验）

\- 第8小时：复盘与小测

第1小时：Kotlin基础微课（讲义+可运行例子）

1\) 变量与基本类型

示例：

```kotlin
fun main() {

    val name: String = "Beijing"

    var age = 20

    val pi = 3.14

    println("name=$name, age=$age, pi=$pi")

    age += 1

    println("new age=$age")

}
```

2\) 字符串模板与转义

```kotlin
fun main() {

    val city = "Beijing"

    val count = 3

    println("I live in $city, visited $count times, length=${city.length}")

}
```

3\) 控制流 if / when / for / while

```kotlin
fun main() {

    val score = 86

    val grade = if (score >= 90) "A" else if (score >= 80) "B" else "C"

    println(grade)



    val day = 3

    val dayName = when (day) {

        1 -> "Mon"

        2 -> "Tue"

        3 -> "Wed"

        in 4..5 -> "Weekday"

        else -> "Weekend"

    }

    println(dayName)



    for (i in 1..5) print("$i ")

    println()

    var x = 3

    while (x > 0) { print("$x "); x-- }

}
```

4\) 函数与默认参数

```kotlin
fun greet(name: String = "Guest", city: String = "Beijing"): String {

    return "Hello $name from $city"

}

fun main() {

    println(greet())

    println(greet("Alice"))

    println(greet(city = "Shanghai", name = "Bob"))

}
```

5\) 可空类型与安全调用

```kotlin
fun safeUpper(input: String?): String {

    // 安全调用?.，Elvis运算符?: 提供默认值

    return input?.uppercase() ?: "(empty)"

}

fun main() {

    println(safeUpper("hi"))

    println(safeUpper(null))

}
```

6\) 标准输入输出（Playground可能受限，了解用法）

```kotlin
fun main() {

    print("Enter name: ")

    val name = readLine()

    println("Hi, ${name ?: "Guest"}")

}
```

第2-3小时：环境搭建与第一个Android项目

A. 安装与加速

\- 安装 Android Studio（稳定版），按向导安装Android SDK与Platform Tools

\- Gradle下载慢：配置代理或镜像

\- SDK Manager 勾选：Android SDK Platform、Build-Tools、Platform-Tools、Google USB Driver

B. Windows真机调试

\- 手机开启开发者模式与USB调试，连接电脑并授权

\- 若未识别，安装厂商驱动或Google USB Driver

\- 命令行检查：

```bash
adb devices
```

C. 创建项目

\- New Project -> Empty Views Activity

\- 勾选 Use Kotlin、Use ViewBinding

\- 建议 minSdk 23/24，target/compile 选择最新稳定

\- 运行到真机，看到Hello World

D. 常见问题速解

\- Gradle慢：代理/镜像；先Sync再Build

\- ADB未识别：换线/换口、装驱动、重启adb

```bash
adb kill-server

adb start-server
```

\- 安装失败：检查USB配置、卸载旧包、确认签名与包名

第4小时：Kotlin小练习

练习1：函数与字符串

需求：写函数 profile(name: String?, age: Int): String

\- name为null或空 -> "Invalid name"

\- age <0 或 >120 -> "Invalid age"

\- 否则 -> "Name=name, Age=age"

参考实现：

```kotlin
fun profile(name: String?, age: Int): String {

    if (name.isNullOrBlank()) return "Invalid name"

    if (age !in 0..120) return "Invalid age"

    return "Name=$name, Age=$age"

}



fun main() {

    println(profile("Tom", 20))

    println(profile(null, 30))

    println(profile("", -1))

    println(profile("Alice", 121))

}
```

练习2：when分支

```kotlin
fun gradeOf(score: Int): String = when (score) {

    in 90..100 -> "Excellent"

    in 80..89 -> "Good"

    in 60..79 -> "Pass"

    in Int.MIN\_VALUE..59 -> "Fail"

    else -> "Invalid"

}



fun main() {

    println(gradeOf(95))

    println(gradeOf(83))

    println(gradeOf(70))

    println(gradeOf(10))

}
```

练习3：循环与聚合

```kotlin
fun evenSquareSum(list: List<Int>): Int =

    list.filter { it % 2 == 0 }

        .map { it \* it }

        .sum()



fun main() {

    println(evenSquareSum(listOf(1,2,3,4,5,6))) // 4^2 + 6^2 = 16 + 36 = 52

}
```

第5小时：Android项目结构与检查清单

A. 结构要点

\- AndroidManifest.xml：组件与权限

\- java/kotlin：源码（后续分包 ui/ data/）

\- res：layout/drawable/values(strings, colors, dimens, styles)

\- build.gradle.kts：依赖、编译配置；ViewBinding开关

B. 让AI提炼用提示词

```text
请用中文输出“Android 项目结构与常见目录说明”的要点清单，
并给出一个最小项目中应当检查的 10 项设置
（例如：minSdk、targetSdk、ViewBinding 开关、AndroidX 启用、依赖版本对齐），
附示例截图说明点（文字描述即可）。
```

第6-7小时：实操（ViewBinding + 交互 + 输入校验）

1\) 启用ViewBinding（创建时已勾选可略）

```kotlin
// app/build.gradle.kts

android {

    buildFeatures {

        viewBinding = true

    }

}
```

2\) 布局 activity\_main.xml

```xml
<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"

    android:orientation="vertical" android:padding="16dp"

    android:layout\_width="match\_parent" android:layout\_height="match\_parent">



    <TextView

        android:id="@+id/tvTitle"

        android:text="Hello World"

        android:textSize="20sp"

        android:layout\_width="wrap\_content" android:layout\_height="wrap\_content"/>



    <EditText

        android:id="@+id/etName"

        android:hint="请输入姓名"

        android:layout\_width="match\_parent" android:layout\_height="wrap\_content"/>



    <Button

        android:id="@+id/btnGreet"

        android:text="打招呼"

        android:layout\_width="wrap\_content" android:layout\_height="wrap\_content"/>

</LinearLayout>
```

3\) MainActivity.kt

```kotlin
package your.package.name



import android.os.Bundle

import android.widget.Toast

import androidx.appcompat.app.AppCompatActivity

import your.package.name.databinding.ActivityMainBinding



class MainActivity : AppCompatActivity() {

    private lateinit var binding: ActivityMainBinding



    override fun onCreate(savedInstanceState: Bundle?) {

        super.onCreate(savedInstanceState)

        binding = ActivityMainBinding.inflate(layoutInflater)

        setContentView(binding.root)



        binding.btnGreet.setOnClickListener {

            val name = binding.etName.text?.toString()?.trim()

            if (name.isNullOrEmpty()) {

                Toast.makeText(this, "请输入姓名", Toast.LENGTH\_SHORT).show()

            } else {

                binding.tvTitle.text = "Hello, $name"

            }

        }

    }

}
```

4\) 扩展函数 Utils.kt（可选）

```kotlin
package your.package.name



fun String?.isValidName(): Boolean =

    !this.isNullOrBlank() \&\& this.length in 1..20
```

使用示例（节选）：

```kotlin
val name = binding.etName.text?.toString()

if (!name.isValidName()) {

    Toast.makeText(this, "姓名不合法（1-20个字符）", Toast.LENGTH\_SHORT).show()

} else {

    binding.tvTitle.text = "Hello, $name"

}
```

常见坑与解法

\- 找不到Binding类：Build -> Clean Project / Rebuild Project；确认布局文件名与生成类名对应（activity\_main -> ActivityMainBinding）

\- TextView不更新：是否使用了同一binding实例；避免多次setContentView

\- 中文输入法顶起布局：先忽略，后续学windowSoftInputMode

第8小时：复盘与小测

A. 复盘模板

\- 今天遇到的3个问题与解决

\- 还不确定的2个点（记录明天追问）

\- 代码是否有空指针风险、主线程阻塞、命名不清

B. 代码审查提示词

我在Kotlin与Android入门的第1天写了一个只有MainActivity的Demo。请你从空指针、生命周期、UI线程访问、命名与可读性、可扩展性五个方面做代码审查。给出具体改进建议与示例代码。

C. 小测（简答）

\- val vs var 的区别与使用建议

\- String? 安全取长度的写法

\- when 与 switch 的差异与区间示例

\- ViewBinding 的主要优势

\- adb devices 显示 unauthorized 的处理步骤

参考要点

\- val不可变优先；提高可读性与降低并发风险

\- str?.length 或 str?.length ?: 0

\- when支持任意类型、区间、条件且是表达式；示例 when(x){ in 1..5 -> ... }

\- 类型安全、避免findViewById、减少空指针

\- 手机确认USB调试授权；adb重启；更换数据线/端口；安装驱动

交付物（今日结束前）

\- 可运行的Debug APK并安装到真机

\- 源码包含ActivityMainBinding使用、按钮交互与输入校验

\- “第1天问题清单与解决方案”文本

\- 若有时间：20-30秒真机演示视频

需要我给你提供Gradle国内镜像模板、或遇到任何编译/运行错误，直接把报错信息或堆栈贴给我，我帮你快速定位。
