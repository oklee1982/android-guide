# **主题**：Kotlin 第一周知识点综合练习题（从简到难 + 新手友好），包括基础语法篇、空安全篇、集合操作篇、面向对象篇和协程篇共 10 道题，分别对 Kotlin 不同方面的知识点进行练习和巩固，如变量常量使用、函数参数设置、控制流判断、空安全操作、集合操作、面向对象设计、协程基础等。

# Kotlin第一周知识点综合练习题（从简到难+新手友好）

所有题目覆盖高频知识点，每道题附「考点」「提示」「分步解答」，新手可按顺序逐步突破，做完就能掌握核心用法～

## 一、基础语法篇（必做，夯实底层）

### 题1：变量与常量的正确使用（考点：val/var、类型推断）

**需求**：定义一个“学生”相关的变量/常量，满足：

1. 学生姓名一旦确定不能修改（用val）

2. 学生年龄可能增长（用var）

3. 学生性别显式声明为String类型，默认值“未知”

4. 打印学生信息：“姓名：XX，年龄：XX，性别：XX”

**提示**：val是只读（初始化后不能改），var是可变；显式声明类型格式：`val 变量名: 类型 = 值`

**解答**：

```Kotlin

```

### 题2：函数的默认参数与命名调用（考点：默认参数、命名参数）

**需求**：写一个“打招呼”函数，满足：

1. 支持传入姓名（必填）、问候语（默认“你好”）、后缀（默认“！”）

2. 调用函数时，分别实现：
   
   - 只传姓名
   
   - 传姓名+自定义问候语
   
   - 传姓名+自定义后缀（用命名参数）

**提示**：默认参数在函数定义时赋值；命名参数调用时用“参数名=值”指定，可跳过默认参数

**解答**：

```Kotlin

```

### 题3：控制流之if表达式与when表达式（考点：if表达式、when多分支）

**需求**：写一个“成绩评级”程序，满足：

1. 输入分数（0-100），用if表达式判断是否及格（≥60），返回“及格”/“不及格”

2. 用when表达式给分数评级：90-100→A，80-89→B，70-79→C，60-69→D，<60→E

3. 打印“分数XX，是否及格：XX，评级：XX”

**提示**：Kotlin的if是表达式（有返回值），可替代三元运算符；when支持区间匹配（`in 80..89`）

**解答**：

```Kotlin

```

## 二、空安全篇（必做，Kotlin核心）

### 题4：空安全操作符（考点：?.、?:、可空类型）

**需求**：模拟“获取用户地址”场景，满足：

1. 定义一个可空的用户名变量（`String?`），可能为null（比如未登录）

2. 定义一个函数`getAddress(username: String?): String?`，逻辑：
   
   - 如果用户名是null，返回null
   
   - 否则返回“${username}的地址：北京市”

3. 调用函数，用`?.`安全调用，用`?:`设置默认值（“未登录，无法获取地址”）

4. 打印最终结果

**提示**：`String?`表示可空类型，`?.`会先判断是否为null，不为null才执行后续操作；`?:`表示“如果左边为null，就用右边的值”

**解答**：

```Kotlin

```

### 题5：作用域函数（考点：let、apply、run）

**需求**：优化“用户信息初始化”代码，满足：

1. 定义一个数据类`User(var name: String, var age: Int, var email: String?)`

2. 用`apply`初始化一个User对象（设置name、age、email）

3. 用`let`处理email（如果为null，设置默认邮箱“[unknown@xxx.com](mailto:unknown@xxx.com)”）

4. 用`run`打印用户完整信息（“姓名：XX，年龄：XX，邮箱：XX”）

**提示**：apply返回对象本身，适合初始化；let用it指代对象，适合处理可空数据；run用this指代对象，适合多步操作

**解答**：

```Kotlin

```

## 三、集合操作篇（必做，高频业务场景）

### 题6：集合基础与高阶函数（考点：listOf、filter、map、sortedBy）

**需求**：处理“商品列表”数据，满足：

1. 定义数据类`Product(val name: String, val price: Double, val category: String)`

2. 创建一个商品列表（至少5个商品，包含不同分类和价格）

3. 完成3个操作：
   
   - 筛选：只保留“电子产品”分类的商品
   
   - 转换：提取筛选后商品的“名称+价格”字符串（如“手机：3999.0”）
   
   - 排序：按价格升序排序

4. 打印最终结果

**提示**：filter是“筛选”（保留满足条件的元素），map是“转换”（把元素变成另一种形式），sortedBy是“按字段排序”

**解答**：

```Kotlin

```

### 题7：序列（Sequence）的延迟计算（考点：asSequence、终止操作）

**需求**：对比集合与序列的差异，满足：

1. 用`listOf`创建一个1-10000的列表

2. 用集合链式操作（filter+map+take(5)），打印执行耗时

3. 用序列操作（asSequence()+filter+map+take(5)+toList()），打印执行耗时

4. 观察两者耗时差异（序列更快，因为延迟计算只处理前5个元素）

**提示**：集合是“即时计算”（每一步都生成新集合），序列是“延迟计算”（直到终止操作才执行，只处理需要的元素）

**解答**：

```Kotlin

```

## 四、面向对象篇（必做，代码设计基础）

### 题8：数据类与接口（考点：data class、Interface）

**需求**：设计“动物”相关类，满足：

1. 定义接口`AnimalAction`，包含两个方法：
   
   - `eat()`：无返回值，打印“吃食物”
   
   - `move()`：返回String，描述移动方式

2. 定义数据类`Dog(val name: String)`，实现`AnimalAction`：
   
   - 重写`eat()`：打印“${name}吃骨头”
   
   - 重写`move()`：返回“${name}跑着走”

3. 定义数据类`Bird(val name: String)`，实现`AnimalAction`：
   
   - 重写`eat()`：打印“${name}吃虫子”
   
   - 重写`move()`：返回“${name}飞着走”

4. 创建Dog和Bird对象，调用方法并打印结果

**提示**：data class自动生成toString等方法；接口可定义行为契约，类可实现多个接口

**解答**：

```Kotlin

```

### 题9：设计原则（单一职责+依赖倒置）

**需求**：重构“订单处理”代码，满足：

1. 原始问题：一个`OrderService`类既负责计算订单金额，又负责保存订单到数据库，违反单一职责

2. 重构要求：
   
   - 拆分`OrderCalculator`类（负责计算金额）
   
   - 拆分`OrderRepository`接口（定义保存订单的契约）和`InMemoryOrderRepository`实现类（内存存储）
   
   - `OrderService`依赖`OrderCalculator`和`OrderRepository`接口（依赖倒置）

3. 测试：创建订单，调用`OrderService`的`processOrder`方法，打印“订单处理完成，金额：XX”

**提示**：单一职责=一个类只做一件事；依赖倒置=依赖抽象（接口）而非具体实现

**解答**：

```Kotlin

```

## 五、协程篇（必做，异步编程核心）

### 题10：协程基础（考点：suspend函数、CoroutineScope）

**需求**：模拟“异步获取数据”，满足：

1. 定义两个挂起函数（`suspend`修饰）：
   
   - `fetchUser(): String`：延迟1秒，返回“用户信息：张三”
   
   - `fetchOrder(): String`：延迟1.5秒，返回“订单信息：2024001”

2. 用`coroutineScope`创建协程作用域，串行执行两个函数

3. 打印执行结果和总耗时（≈2.5秒）

**提示**：suspend函数只能在协程或其他suspend函数中调用；coroutineScope会等待所有子协程完成

**解答**：

```Kotlin

```

### 题11：协程并行（考点：async、awaitAll）

**需求**：优化题10，用并行执行减少耗时，满足：

1. 复用题10的`fetchUser`和`fetchOrder`函数

2. 用`async`创建两个并行协程，同时获取用户和订单信息

3. 用`awaitAll`等待两个协程完成，获取结果

4. 打印执行结果和总耗时（≈1.5秒，取最长任务时间）

**提示**：async用于启动并行协程，返回Deferred对象；awaitAll等待所有并行协程完成，返回结果列表

**解答**：

```Kotlin

```

## 六、综合项目篇（终极巩固，必做）

### 题12：个人待办CLI程序（整合所有知识点）

**需求**：实现一个命令行待办程序，支持添加、查看、完成、退出功能，满足：

1. 用数据类`Todo(val id: Int, val title: String, var isDone: Boolean)`存储待办

2. 用密封类`Command`定义支持的命令：`Add(title: String)`、`List`、`Done(id: Int)`、`Exit`

3. 用`CommandParser`类解析用户输入（单一职责）

4. 用`TodoRepository`接口+`InMemoryTodoRepository`实现待办存储（依赖倒置）

5. 用`TodoService`处理业务逻辑（依赖Repository）

6. 循环接收用户输入，执行对应命令，直到输入“exit”退出

**提示**：结合前面的所有知识点：数据类、接口、设计原则、控制流、集合操作、空安全

**解答**：

```Kotlin

```

## 练习建议

1. 按顺序做题：先基础语法→空安全→集合→OOP→协程→综合项目，每道题先自己写，卡壳再看提示和解答

2. 动手修改：每道题做完后，尝试修改需求（比如题12添加“删除待办”功能），巩固知识点

3. 重点关注：综合项目（题12）是核心，它整合了所有高频知识点，一定要独立完成一遍

4. 遇到问题：优先用`println`调试，或查看Kotlin官方文档（[https://kotlinlang.org/docs/home.html](https://kotlinlang.org/docs/home.html)）

如果某道题卡壳严重，或对某个知识点理解不深，可以随时问我，我会针对性讲解！

> （注：文档部分内容可能由 AI 生成）